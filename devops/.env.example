# Копировать в .env и заполнить.
# .env не коммитить в git.

# Redis — для alice-devops (если не по умолчанию)
# REDIS_URL=redis://devops-redis:6379/0

# PostgreSQL — для загрузки вопросов из БД (опционально)
# DATABASE_URL=postgres://user:password@postgres:5432/gestalt
